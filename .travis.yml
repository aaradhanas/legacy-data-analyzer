matrix:
  include:
    - language: java
      before_install:
        - chmod +x mvnw
      install:
        - ./mvnw install -DskipTests=true
      script:
        - ./mvnw compile

    - language: node_js
      node_js:
        - "6.11"
      install:
        - yarn install
      script:
        - yarn build